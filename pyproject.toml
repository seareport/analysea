[tool.poetry]
name = "analysea"
version = "0.1.0"
description = ""
readme = "README.md"
authors = ["Thomas Saillour <saillour.thomas@gmail.com>"]

[tool.poetry.dependencies]
python = ">=3.10,<4.0"
pandas = "*"
ruptures = "*"
utide = "*"
xarray = {version = "*", extras = ["io", "accel"]}

[tool.poetry.group.dev.dependencies]
fastparquet = "*"
ipykernel = "*"
ipympl = "*"
mypy = "*"
pytest = "*"
pytest-cov = "*"
pytest-notebook = "*"

[build-system]
requires = ["poetry-core>=2.0.0", "setuptools>=65", "wheel"]
build-backend = "poetry.core.masonry.api"

[tool.ruff]
target-version = "py38"
line-length = 108
select = [
    "E",  # pycodestyle
    "F",  # pyflakes
    "C90",  # mccabe
]
ignore = [
    "E501", # line-too-long
]

[tool.black]
line-length = 108
target-version = ['py310']

[tool.mypy]
python_version = "3.10"
show_error_codes = true
show_column_numbers = true
show_error_context = true
warn_no_return = true
warn_redundant_casts = true
warn_return_any = true
warn_unreachable = true
warn_unused_ignores = true
strict = true
ignore_missing_imports = true
